- repository, service e controller login
- banco de dados login

rodar testes
go test -v -tags=integration ./...

rodar docker
 sudo docker compose -f docker-compose.dev.yml  --env-file .env up -d --build


Dica para o seu código Go:
Como você está usando o Go 1.26, aproveite para salvar no banco de dados não só a imagem da assinatura, 
mas também o IP e o User-Agent (navegador/dispositivo) de quem assinou.
 Isso se chama Trilha de Auditoria e dá 10x mais valor ao seu software se houver uma perícia judicial.